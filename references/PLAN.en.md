> Sync status: This English document may lag behind the Chinese version (.md).

# Plan

## v0.1 Completed

- Single skill entry (`SKILL.md`)
- Skill shell scripts (`scripts/`)
- Three adapters: `xiaohongshu`, `wechat`, `generic_html`
- Default fetch-only mode with optional `--store`

## Next

1. Further clean up WeChat article body extraction.
2. Stabilize report output format for LLM summarization.
3. Add new platform adapters incrementally when needed.
